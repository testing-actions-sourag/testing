name: Discussion Workflow Run

on:
  workflow_run:
    workflows: ["Pull Request"]
    types:
      - completed
    
jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
      - uses: souragc/myaction2@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
