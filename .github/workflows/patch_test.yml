name: testing patch

on:
  issue_comment:
  
jobs:
  one:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "${{ github.event.comment.body }}"
        env:
          BODY: ${{ github.event.comment.body }}
